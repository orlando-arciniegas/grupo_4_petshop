<!DOCTYPE html>
<html lang="en">
<head>
    <!------------ HEAD  ---------->
    <%- include  ('../partials/head.ejs') %>
    <link rel="stylesheet" href="/css/detalle.css">
    <title>Productos | Pets</title>
</head>
<body>
    <!--HEADER -->
    <%- include  ('../partials/header.ejs') %>

    <!-- MAIN -->
    <main class="main">
        <figure class="figure">
            <img class="imgProd" src="/img/productos/<%= producto.imagen %> " alt="<%= producto.nombre %>">
            <img class="miniProd" src="/img/productos/<%= producto.imagen %> " alt="mini <%= producto.nombre %>">
        </figure>
        <div class="detProd">
            <h2 class="detalle"><%= producto.nombre %>.</h2>
            <h2 class="precioProd">$ <%= producto.precio %></h2>
        </div>
        <div class="agregar">
            <button class="button" type="submit">AGREGAR AL CARRITO</button>
            <div class="detalles-cantidad detalleCant">
                <button type="" class="menos">-</button>
                <input type="text" value="1">
                <button class="mas">+</button>
            </div>
        </div>
    </main>

    <!-- Footer -->
    <%- include  ('../partials/footer.ejs') %>
</body>
</html>